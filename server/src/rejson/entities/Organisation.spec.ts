describe('Organisation', () => {
  it('Should create an organisation', () => {});

  it('Should add a squad to an organisation', () => {});

  it('Should remove a squad from an organisation', () => {});

  it('Should change properties of an organisation', () => {});

  it('Should add a person to an organisation', () => {});

  it('Should remove a person from an organisation', () => {});
});
